# If you just change the port or add more ports here, you will likely also
# have to change the VirtualHost statement in
# /etc/apache2/sites-enabled/000-default.conf
Listen 8182

<IfModule ssl_module>
	Listen 443
</IfModule>

<IfModule mod_gnutls.c>
	Listen 443
</IfModule>

LoadModule wsgi_module "/home/xuchd/.py3env_mccloud/lib/python3.5/site-packages/mod_wsgi/server/mod_wsgi-py35.cpython-35m-x86_64-linux-gnu.so"
WSGIPythonHome "/home/xuchd/.py3env_mccloud"

WSGIRestrictStdout Off
XSendFile on
XSendFilePath /home/xuchd/mccloud/data/protected
User  xuchd
Group MCDC


<VirtualHost *:8182>

	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	ServerName mccloud
	ServerAdmin xuchenghai@gmail.com
	DocumentRoot /home/xuchd/mccloud

	SSLEngine on
	SSLCertificateFile /home/xuchd/mccloud/deploy/server.crt
	SSLCertificateKeyFile /home/xuchd/mccloud/deploy/server.key 

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

	ErrorLog /home/xuchd/mccloud/log/error.log
	CustomLog /home/xuchd/mccloud/log/access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf

    WSGIDaemonProcess django python-home="/home/xuchd/.py3env_mccloud" python-path="/home/xuchd/.py3env_mccloud/lib/python3.5/site-packages"


    WSGIProcessGroup django
    WSGIApplicationGroup %{GLOBAL}

    Alias /static /home/xuchd/mccloud/collected_static
    <Directory /home/xuchd/mccloud/collected_static>
    Require all granted
    </Directory>


    WSGIScriptAlias / /home/xuchd/mccloud/mccloud/wsgi.py
    <Directory /home/xuchd/mccloud>
    <Files wsgi.py>
    Require all granted
    </Files>
    </Directory>


</VirtualHost>

# vim: syntax=apache ts=4 sw=4 sts=4 sr noet
